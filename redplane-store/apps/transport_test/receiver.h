#include "common.h"

DEFINE_bool(process_req, false, "Process requests (true: receive and process, false: just receive)");
DEFINE_bool(batch, false, "Request batch (true: batch requests, false: no batch request)");
